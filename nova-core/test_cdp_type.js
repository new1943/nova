const puppeteer = require('puppeteer-core');
const fs = require('fs');

(async () => {
  // Let's not run puppeteer if it's not installed. 
  // Let's just update the Rust code which connects via websockets.
})();
