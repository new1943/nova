# NOVA 开发会话记录 — 2026-04-08

## 已完成的修复

### 1. 401 认证问题
- **原因**: `~/.nova/config` 有 `[default]` section header，TOML 解析时 api_key 落入子表，顶层为空
- **修复**: 去掉 `[default]`，改为顶层键值对

### 2. 模型自称 Claude
- **原因**: 无 IDENTITY.md，MiniMax M2.7 默认自称 Claude
- **修复**: 创建 `~/.nova/IDENTITY.md` 声明身份为 NOVA

### 3. MiniMax M2.7 thinking block 支持
- `nova-api/src/types.rs`: ContentBlock/ContentBlockStartData/DeltaData 增加 Thinking 变体
- `nova-api/src/client.rs`: SSE handler 处理 Thinking 事件

### 4. TUI 修复
- `nova-tui/src/ui.rs`: 滚动计算考虑 Wrap 折行、Tool prefix 临时引用修复、budget 进度条
- `nova-tui/src/app.rs`: 增加 scroll_offset 支持手动滚动
- `nova-tui/src/input.rs`: Up/Down/PageUp/PageDown 滚动、/quit /new 命令支持
- `nova-daemon/src/main.rs`: budget_pct 实际计算（不再写死 0.0）

### 5. P0 正确性修复（对照 requirements/design/tasks 文档）
- **GAP 1**: budget check 移到 API 调用前（pre-flight check）
- **GAP 2**: context-overflow 自动 compact + retry
- **GAP 3**: record_turn 移到 check 之后（边际递减检测修复）
- **GAP 23**: QueryLoop 内 assistant/tool 消息持久化到 JSONL（daemon 在 loop 结束后 diff 写入）
- **GAP 24**: PostSampling hook 改用 write_if_not_written，StopHook 改用 write_and_mark（双写互斥修复）

### 6. P1 功能补全
- **GAP 5**: Compact target_pct 实际使用（按 context_window * target_pct 计算保留量）
- **GAP 7**: ForkedAgent 重试间加 1s sleep
- **GAP 12/13**: bash 工具安全改进（去掉过于激进的 > 拦截，增加 ALLOWED_COMMANDS 白名单）
- **GAP 15**: daemon PID 文件（/tmp/nova.pid）
- **GAP 17**: Skills 系统接入 daemon（/skill-name 命令 + auto-trigger）
- **GAP 19**: TUI / 命令支持
- **GAP 20**: TUI 上下滚动
- **GAP 22**: 状态栏 ASCII 进度条

## 当前状态

### 16 策略实现进度
- ✅ 完整可用: 1(QueryLoop) 2(Budget) 3(Compact) 4(ForkedAgent) 7(双写互斥) 8(工具排序) 16(Session JSONL)
- ⚠️ 框架+占位逻辑: 5(PostSampling) 6(StopHooks) — hooks 跑得通但记忆提取是启发式占位
- 🔧 骨架未接入: 9(Team) 10(Subagent) 11(SideQuery) 12(autoDream) 13(Worktree) 14(Coordinator) 15(PasteStore)

### 关键文件变更清单
```
nova-api/src/types.rs        — thinking block 支持
nova-api/src/client.rs       — SSE thinking 处理
nova-core/src/agent/loop.rs  — pre-flight budget, overflow retry, record_turn 顺序
nova-core/src/agent/forked.rs — retry sleep
nova-core/src/token/compact.rs — target_pct 实际使用
nova-core/src/hooks/post_sampling.rs — write_if_not_written
nova-core/src/hooks/stop.rs  — write_and_mark
nova-core/src/tools/bash.rs  — 白名单模式
nova-daemon/src/main.rs      — PID文件, skills集成, JSONL持久化, budget_pct计算
nova-tui/src/app.rs          — scroll_offset
nova-tui/src/ui.rs           — 进度条, 滚动修复
nova-tui/src/input.rs        — 滚动, /命令
~/.nova/config               — 去掉 [default]
~/.nova/IDENTITY.md          — 新建
```

### 待完成
- Phase 2/3 策略 9-15 接入主流程
- PostSampling/StopHook 真正的 LLM 记忆提取（替换启发式占位）
- Heartbeat 接入 daemon
- Sandbox 策略接入工具
- RetryPolicy 接入 API 调用
- TUI 打字机效果（char_delay_ms）
- TUI Markdown 渲染
